const API_HOST = 'localhost';

const ROLES = {
    ADMIN: 'admin',
    WRITER: 'writer',
    READER: 'reader',
}

const VIDEO_APP_PORT = 8081;

const WS_TIMEOUT = 10000;

module.exports = {
    ROLES,
    API_HOST,
    VIDEO_APP_PORT,
    WS_TIMEOUT,
}
