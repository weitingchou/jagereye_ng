const API_HOST = 'localhost';

const ROLES = {
    ADMIN: 'admin',
    WRITER: 'writer',
    READER: 'reader',
}

const WS_TIMEOUT = 10000;

module.exports = {
    ROLES,
    API_HOST,
    WS_TIMEOUT,
}
